# Route Removal / Merge Implementation Prompt

Priority structural changes:
- Split `/faq` and `/help`.
- Keep `/support` and `/guarantee` as redirects only.
- Deindex or remove broken non-core product routes.
- Replace `/partners` and `/locator` with truthful equivalents.
