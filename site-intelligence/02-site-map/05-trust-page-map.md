# Trust Page Map

- `/research` is the strongest current evidence surface.
- `/about` should be the main entity-trust page but is currently underbuilt.
- `/shipping`, `/returns`, `/warranty`, `/privacy`, `/terms`, and `/cookie-policy` are the strongest current trust templates.
- `/press` and `/locator` are current trust risks, not trust assets.
