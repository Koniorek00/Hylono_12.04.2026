# Redundant / Weak / Merge / Delete Candidate Map

- Split `/faq` and `/help` into distinct routes.
- Keep `/support` as a redirect only.
- Keep `/guarantee` as a redirect only and remove the dead page behind it.
- Remove secondary broken product routes from the sitemap until rebuilt.
- Replace `/partners` with a real partner-program page or redirect to a clearer public/private split.
