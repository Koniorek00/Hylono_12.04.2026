/**
 * ExitIntent — detects when the user's cursor moves toward the top of the
 * viewport (exit intent signal) and shows a retention offer overlay.
 *
 * Currently a stub — full implementation deferred to TASK-016.
 */
import React from 'react';

export const ExitIntent: React.FC = () => {
    // TODO: Implement exit intent detection and overlay (TASK-016)
    return null;
};
